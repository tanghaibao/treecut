#!/usr/bin/env python
# -*- coding: UTF-8 -*-

from distutils.core import setup #, Extension


setup(
      name="dynamic-treecut",
      packages=['dynamic-treecut'],
      requires=['fisher'],
      )
